<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!--
        Приложение полностью офлайн. Readium тянет INTERNET в манифест транзитивно —
        убираем, чтобы APK не просил лишнего (см. раздел 12 ТЗ).
    -->
    <uses-permission android:name="android.permission.INTERNET" tools:node="remove" />

    <!--
        WorkManager приносит с собой четыре разрешения. Два из них нам не нужны:
        работа с уведомлением на переднем плане не запускается нигде, а
        перепланирование после перезагрузки не требуется — импорт живёт в очереди
        в базе и продолжится при следующем запуске.
        Остаются WAKE_LOCK (без него воркер падает с SecurityException) и
        ACCESS_NETWORK_STATE (читает только состояние сети; выхода в сеть нет,
        INTERNET удалён выше).
    -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" tools:node="remove" />
    <uses-permission
        android:name="android.permission.RECEIVE_BOOT_COMPLETED"
        tools:node="remove" />

    <application
        android:name=".BibliariumApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/Theme.Bibliarium">

        <activity
            android:name=".ui.MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.Bibliarium"
            android:windowSoftInputMode="adjustResize">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>
</manifest>
